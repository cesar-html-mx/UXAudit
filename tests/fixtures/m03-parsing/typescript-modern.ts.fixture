interface AuditOptions {
  readonly enabled?: boolean;
}

type AuditValue<T> = {
  readonly value: T;
};

export const defaultOptions = {
  enabled: true,
} satisfies AuditOptions;

export const wrapValue = <T>(value: T): AuditValue<T> => ({ value });
